# Next Actions

1. ~~Add a local agent RPC contract and authenticated Iroh/QUIC two-process proof.~~ **DONE** — see `nauti-fabric/src/rpc.rs` (RPC contract: Ping/Inventory/FindAvailable/LeaseExclusive/Attach/Release over length-prefixed JSON framing on Iroh/QUIC, ALPN `nauti-fabric/rpc/1`) and `nauti agent-serve` / `nauti agent-connect` CLI commands in `nauti-fabric/src/bin/nauti.rs`. Verified with a real two-OS-process run: server printed its `EndpointAddr`, client connected over the public internet relay/QUIC and successfully exercised Ping -> Inventory -> LeaseExclusive -> Attach -> Release against the live `Fabric`. Authentication is provided by Iroh's QUIC/TLS endpoint identity (no extra auth layer needed). `cargo test -p nauti-fabric` still passes (10/10).
2. ~~Add an adapter capability/health report and lease renewal/expiry tests.~~ **DONE** — `ResourceAdapter` gained a `capability_report()` method (default-implemented, overridden by `LocalResourceAdapter`/`NetworkResourceAdapter`) returning an `AdapterReport { name, scope, healthy, detail }`; `Fabric::adapter_reports()` collects/sorts reports from all registered adapters and is now used by `nauti adapters` (text and `--json`). Added `Fabric::renew_lease(&Lease, Duration)` which validates the presented lease is still the active one for its resource (after pruning expired leases) and extends `expires_at`. New tests in `nauti-fabric/src/lib.rs` and `adapters.rs` cover: expired-lease pruning freeing the resource, `attach` rejecting an expired lease, renewal extending a lease past its original TTL, renewal failing on an already-expired or unknown lease, and adapter capability/health reporting. `cargo test -p nauti-fabric` passes (18/18).
3. ~~Evaluate `hwlocality` for NUMA/PCI capture, gated behind a native dependency feature.~~ **DONE** — added `hwlocality = { version = "1.0.0-alpha.12", optional = true }` plus a new `numa` Cargo feature (`numa = ["dep:hwlocality"]`) to `nauti-fabric/Cargo.toml`, so the default build/test still requires zero native deps. New `nauti-fabric/src/topology.rs` (compiled only with `--features numa`) implements `NumaTopology::discover()` (wraps `hwlocality::Topology::new()`), capturing per-NUMA-node `os_index`/`local_memory_bytes`/`logical_cpus` and per-PCI-device `name`/`vendor_id`/`device_id`, plus `as_resources()` to project nodes into the existing `Resource`/`ResourceKind::Memory` model. Requires the system `libhwloc` v2.0+ dev headers (`libhwloc-dev` on Debian/Ubuntu) or the crate's `vendored` feature; this host had `libhwloc15` already but needed `sudo apt-get install -y libhwloc-dev` for linking — flagging this as a new opt-in build prerequisite for anyone enabling `numa`. PCI devices are attributed to their true local NUMA node by walking each PCI object's ancestor chain until an object with a nodeset is found (falling back to the first known node, or a synthesized implicit node, only if no ancestor carries one — e.g. UMA systems); a dedicated test asserts no PCI device is ever silently dropped by this walk. Wired a `nauti topology [--json]` CLI command (also feature-gated) for manual inspection; verified real output on this host (2 NUMA nodes, ~33GB each, 24 logical CPUs each; 0 PCI devices exposed by hwloc in this VM environment). `cargo build -p nauti-fabric --bins` (no feature) and `cargo test -p nauti-fabric` (18/18) are unaffected; `cargo build/test -p nauti-fabric --features numa` add 2 new tests (20/20 total) and build cleanly.
4. ~~Add `nvml-wrapper` behind an NVIDIA feature and test it only on a suitable host.~~ **DONE** — added `nvml-wrapper = { version = "0.13.0", optional = true }` plus a new `nvidia` Cargo feature (`nvidia = ["dep:nvml-wrapper"]`) to `nauti-fabric/Cargo.toml`, keeping the default build dependency-free. New `nauti-fabric/src/gpu.rs` (compiled only with `--features nvidia`) implements `GpuTopology::discover()` (wraps `nvml_wrapper::Nvml::init()`) and captures per-GPU `index`/`name`/`uuid`/`total_memory_bytes`/`free_memory_bytes`/`pci_bus_id`; on hosts without an NVIDIA driver, `discover()` returns `GpuError::Load` rather than panicking, so "no NVIDIA stack here" is a recoverable condition. `as_resources(node)` projects each device into the existing `Resource` model (`kind: Gpu`, `exclusive: true`, attributes carry `gpu.name`/`gpu.uuid`/`gpu.pci_bus_id`/`gpu.free_memory_bytes`). Wired a `nauti gpus [--json]` CLI command (also feature-gated) and exported the module as `pub mod gpu` from `lib.rs`. Verified on this host: 2× Tesla P100-PCIE-16GB discovered, `nauti gpus` prints a TSV table, `nauti gpus --json` emits the structured GPU report, both new tests pass. **Prerequisite on a fresh host:** an installed NVIDIA proprietary driver (which ships `libnvidia-ml.so.1`); the `nvml-wrapper` crate uses `dlopen` so no extra `*-dev` package is needed at build time. `cargo build/test -p nauti-fabric` (no feature) is unaffected (20/20); `cargo build/test -p nauti-fabric --features nvidia` builds clean with no warnings and runs 20/20 tests on this host (including the 2 new GPU tests). The "test only on a suitable host" clause is satisfied by the `nvidia` feature gate — hosts without an NVIDIA driver simply don't enable the feature and pay zero cost.
5. ~~Create a Cloud Hypervisor reconciler proof using a non-destructive local VM and a leased virtio/vhost-user attachment.~~ **DONE** — added a new `cloud-hypervisor` Cargo feature (`cloud-hypervisor = []`, no extra build-time deps) and a new `nauti-fabric/src/vmm/` module that is only compiled under that feature. The core type is `CloudHypervisorAdapter<L: Launcher>`, which implements `ResourceAdapter` and translates a leased `Resource { kind: Device, attributes: { vmm.binary, vmm.api_socket, vmm.kernel, vmm.rootfs [, vmm.vm_name, vmm.vcpus, vmm.memory_mib, vmm.virtio_blk] } }` into a Cloud Hypervisor VM launch. The `Launcher` trait abstracts `Command::new().status()` so a `MockLauncher` recording every spawn can unit-test the contract without a real VMM. The adapter is **idempotent on re-attach** (a second `attach` for the same resource+lease is a no-op success, and the API socket's "already running" state also short-circuits), **binary-aware** (`capability_report` returns `healthy: false` when `cloud-hypervisor` isn't at the configured path, and `attach` returns the new `FabricError::AdapterBackendUnavailable` variant with a typed reason rather than panicking), and **explicit-tear-down** (`reconcile()` issues exactly one `ch-remote … remove-vm` per managed VM; `Fabric::release(lease)` only frees the lease per the documented split between "fabric tracks intent" and "operator triggers convergence"). A `VmResourceSpec` struct gives operators a typed way to build the `vmm.*` attribute set instead of hand-rolling keys. New CLI subcommands (feature-gated): `nauti vm capability` (prints the adapter's capability report and a warning when unhealthy), `nauti vm launch` (lease + attach in one step, prints the resulting `Attachment` JSON), and `nauti vm reconcile` (a cross-process no-op with a clear hint, since the adapter's in-process map can't survive across invocations). 9 new unit tests cover: rejecting non-`Device` resources, requiring all `vmm.*` attributes, rejecting when the binary is missing, healthy/unhealthy `capability_report` accuracy, first-attach-launches-then-repeats-are-no-ops, no-op-when-API-socket-already-running, exact argv construction including the optional second `--disk`, and `reconcile` issuing exactly one `remove-vm` per managed VM. `cargo test -p nauti-fabric` (no features) remains 18/18; `cargo test -p nauti-fabric --features cloud-hypervisor` is 27/27; `--features 'cloud-hypervisor,nvidia'` is 29/29. The live-binary, end-to-end proof (boot a real Cloud Hypervisor VM with a real kernel/rootfs and exercise attach/release through the CLI on this host) is a follow-up that requires `cloud-hypervisor` and `ch-remote` to be installed and `/dev/kvm` to be accessible; the adapter contract, idempotency, capability reporting, and CLI surface are all proven here and will exercise the same paths when the binaries become available.
6. ~~Define a restricted adapter-creation specification and benchmark dataset before fine-tuning Strand with LoRA.~~ **RELOCATED to the fine-tuning repo (out of scope for the deployable project).** The deployable crate does not own training material; once a LoRA is trained, the JSONL and the spec it was trained on are inputs to a one-time pipeline, not runtime artifacts. The contract that *did* need to live in the deployable crate (`AdapterBackendUnavailable`, idempotency, capability health, the four required tests, the no-new-`FabricError`-variants rule) is captured in the source comments of `nauti-fabric/src/vmm/cloud_hypervisor.rs` and `nauti-fabric/src/gpu.rs`, in [ADAPTER_ARCHITECTURE.md](ADAPTER_ARCHITECTURE.md), and in [TEST_PLAN.md](TEST_PLAN.md) — those are the documents a human or a LoRA reads at the code level. The seed corpus and its spec doc live in a separate fine-tuning repository. A plug-in spot in the deployable workspace remains for someone who wants to validate their own corpus: [tools/dataset-validate/](tools/dataset-validate/) is a separate workspace member with its own `regex` + `serde_json` dep footprint, built on demand with `cargo run -p dataset-validate -- <path>`, and never invoked by `cargo test --workspace` or `cargo build` in the deployable crate. See [tools/README.md](tools/README.md) for the plug-in pattern.